/**
 * Field types present on yup.
 */
const fieldTypes = {
  string: 'string',
  boolean: 'boolean',
  number: 'number',
  date: 'date',
  object: 'object',
  array: 'array',
};

export default fieldTypes;
