/**
 * Map of yup field properties.
 */
const yupSchemaFieldProperties = {
  /**
   * Field type.
   */
  type: '_type',

  /**
   * Field label, if defined.
   */
  label: '_label',

  /**
   * Defines if the field is exclusive.
   */
  exclusive: '_exclusive',
};

export default yupSchemaFieldProperties;
